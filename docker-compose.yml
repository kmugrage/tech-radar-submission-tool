services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist submissions across restarts
      # Mount the whole data/ directory so atomic rename (tmp.replace) works on Linux
      - ./data:/app/data
